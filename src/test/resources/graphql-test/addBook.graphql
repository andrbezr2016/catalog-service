mutation addBook {
    addBook(
        bookInput: {title: "Test Title"}
    ) {
        author
        description
        id
        isbn
        pages
        publisher
        tags {
            name
            id
        }
        title
        yearPublished
    }
}